@model IEnumerable<Tto>

@{
    ViewData["Title"] = "Index";
}

<h1>Tto Düzenleme Sayfası</h1>

<p>
    <a asp-action="Create">Yeni ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TtoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TtoContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilePath)
            </th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TtoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TtoContent)
            </td>
                <td>
                    <a asp-action="DownloadFile" asp-route-filePath="@item.FilePath">@item.FilePath.Split('\\').Last()</a>
                </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Düzenle</a> |
              
                <a asp-action="Delete" asp-route-id="@item.Id">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
